lim = int(input("Enter the limit to generate Fibonacci sequence: "))

seq = [0, 1]
while seq[-1] + seq[-2] <= lim:
    seq.append(seq[-1] + seq[-2])

print("Fibonacci sequence up to", lim, "is:", seq)
